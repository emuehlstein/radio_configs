# DM-32 target

Renders `ResolvedLayout` → `dmrconfig`-compatible text codeplug for the
BTECH DM-32.

Depends on Eric's DM-32 support patch to `dmrconfig` (in flight).

Not yet implemented. Reference:
- `dmrconfig` upstream: <https://github.com/OpenRTX/dmrconfig>
- `generate_dm32_channels.py` in `OpenGD77_SSRFLite_Generator` already
  produces DM-32-flavored channel data — port that shape here.
